import setuptools

setuptools.setup(
    name="vibe",
    version="0.2",
    author="Muhammed Kocabas",
    description="RT-VIBE: Real-Time VIBE Inference",
    packages=setuptools.find_packages(),
    python_requires='>=3.7',
)
